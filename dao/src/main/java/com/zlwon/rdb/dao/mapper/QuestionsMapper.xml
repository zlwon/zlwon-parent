<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zlwon.rdb.dao.QuestionsMapper" >
    <resultMap id="QuestionsEntity" type="com.zlwon.rdb.entity.Questions" >
        <id column="id" property="id" />
        <result property="nsid" column="nsid"/>
        <result property="uid" column="uid"/>
        <result property="iid" column="iid"/>
        <result property="infoClass" column="info_class"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="createTime" column="create_time"/>
        <result property="examine" column="examine"/>
    </resultMap>

    <!-- 根据提问ID查询提问 -->
    <select id="findQuestionsById" resultMap="QuestionsEntity" parameterType="java.lang.Integer">
        select
        id, nsid, uid, iid, info_class ,title, content, create_time, examine 
        from zl_questions 
        where id = #{id}
    </select>
    
    <!-- 根据用户ID查询其所有的提问 -->
    <select id="findQuestionsByUId" resultMap="QuestionsEntity" parameterType="java.lang.Integer">
        select
        id, nsid, uid, iid, info_class ,title, content, create_time, examine 
        from zl_questions 
        where uid = #{uid}
    </select>
    
    <!-- 根据信息ID查询问题数量 -->
    <select id="countQuestionsByInfoId" resultType="java.lang.Integer">
        select
        count(*) 
        from zl_questions
        where iid = #{infoId} and info_class = #{type} and examine = 1 
    </select>
    
</mapper>